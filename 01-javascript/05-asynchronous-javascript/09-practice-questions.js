/*
ASYNC JAVASCRIPT PRACTICE — ATTEMPT BEFORE OPENING SOLUTIONS

1. Create delay(ms) that resolves after ms milliseconds.
2. Create getUser(id) that rejects when id is missing.
3. Use async/await and try/catch to call getUser.
4. Predict the order of sync logs, Promise callbacks, and timers.
5. Run three independent fake requests with Promise.all.
6. Run three requests with allSettled when one fails.
7. Create a timeout using Promise.race.
8. Compare sequential and parallel timings.
9. Write a fetch helper that checks response.ok.
10. Explain why an async function always returns a Promise.
*/

console.log("Write and run your solutions below.");

