/* ADVANCED PRACTICE — ATTEMPT BEFORE OPENING solutions.js */

// 1. flattenDeep(values): recursively flatten an array of unknown depth.

// 2. debounce(callback, delay): preserve this and arguments and expose cancel().

// 3. promiseAll(iterable): preserve input order, accept normal values, resolve
//    empty input, and reject on the first rejection.

// 4. retry(operation, attempts, delay): retry a failing Promise operation and
//    throw the last error after all attempts.

// 5. buildDashboardSummary(transactions): return income, expense, balance,
//    category expense totals, transaction count, and highest expense in one
//    reduce operation.

console.log("Write your five solutions below and explain complexity.");

