# Patterns Quick Recall

- Lift shared state to the nearest common parent.
- Keep one source of truth for coordinated values.
- Composition uses nested components and `children`.
- Prefer composition over UI inheritance.
- Extract reusable components only for meaningful repetition.
- Custom hooks reuse stateful logic, not shared state instances.
- Custom-hook names begin with `use` and follow hook rules.

