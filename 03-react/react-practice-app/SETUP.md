# Apply the Practice Module

Merge `src` into the app. In `src/App.jsx`, import:

```jsx
import PracticePage from "./pages/PracticePage.jsx";
```

Replace the `/practice` placeholder route with:

```jsx
<Route path="practice" element={<PracticePage />} />
```

